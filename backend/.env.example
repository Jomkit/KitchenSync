# Runtime environment: development | test | staging | production
APP_ENV=development

# API runtime
# HOST=0.0.0.0
# PORT=5000
# FLASK_DEBUG=0

# Preferred in staging/production: provide full DB URL from secret manager/platform.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/kitchensync

# Optional component-based DB configuration (used when DATABASE_URL is not set).
# In APP_ENV=production or APP_ENV=staging, DB_HOST/DB_NAME/DB_USER/DB_PASSWORD are required.
# DB_DRIVER=postgresql+psycopg2
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=kitchensync
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_SSLMODE=disable

# Test database URL (used when APP_ENV=test).
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5433/kitchensync_test

# Optional component-based test DB configuration.
# TEST_DB_DRIVER=postgresql+psycopg2
# TEST_DB_HOST=localhost
# TEST_DB_PORT=5433
# TEST_DB_NAME=kitchensync_test
# TEST_DB_USER=postgres
# TEST_DB_PASSWORD=postgres
# TEST_DB_SSLMODE=disable
