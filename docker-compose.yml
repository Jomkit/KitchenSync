services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: kitchensync
      POSTGRES_PASSWORD: kitchensync
      POSTGRES_DB: kitchensync
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
